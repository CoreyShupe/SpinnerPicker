# Frontend configuration. Copy to frontend/.env to override. Defaults match vite.config.ts.
# Base URL the browser uses to reach the backend API.
VITE_API_URL=http://localhost:8787
# Vite dev server host/port.
FRONTEND_HOST=127.0.0.1
FRONTEND_PORT=5173
