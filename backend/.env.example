# Backend configuration. Copy to backend/.env to override. All values are optional;
# the defaults shown here match src/config.ts.
HOST=127.0.0.1
PORT=8787
DATABASE_PATH=./data/spinner.db
CORS_ORIGIN=http://localhost:5173
